# Example .env file for github-utils scripts

# GitHub personal access token with repo and workflow permissions (required)
GITHUB_TOKEN="ghp_xxxxxxxx"

# The following are only needed if you want to override auto-detection (normally inferred):

# Remote name for git (default: origin, else first remote, used by squash-history.sh)
# GH_REPO_REMOTE="origin"

# Branch name to squash to (default: current branch, used by squash-history.sh)
# GH_REPO_BRANCH="main"

# Optionally override repo owner/name (normally inferred from git remote)
# GH_REPO_OWNER="your-github-username"
# GH_REPO_NAME="your-repo-name"
